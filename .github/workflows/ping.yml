name: Keep Render Awake

on:
  schedule:
    - cron: '30,45 6 * * 1-6'
    - cron: '0,15,30,45 7-20 * * 1-6'
    - cron: '0,15 21 * * 1-6'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    timeout-minutes: 3

    steps:
      - name: Scarica log precedente da Gist
        continue-on-error: true
        run: |
          GIST_ID="${{ secrets.GIST_ID }}"
          
          if [ -n "$GIST_ID" ]; then
            echo "Scarico log da Gist $GIST_ID..."
            
            # Scarica il contenuto del Gist
            RESPONSE=$(curl -s -H "Authorization: token ${{ secrets.GIST_TOKEN }}" \
              "https://api.github.com/gists/$GIST_ID")
            
            # Estrai il contenuto del file
            echo "$RESPONSE" | jq -r '.files["ping-log.txt"].content' > log.txt 2>/dev/null
            
            # Verifica se il download è riuscito
            if [ -s log.txt ]; then
              echo "✅ Log scaricato: $(wc -l < log.txt) righe"
              # Mantieni solo le ultime 500 righe
              tail -n 500 log.txt > log_temp.txt && mv log_temp.txt log.txt
            else
              echo "⚠️ Gist vuoto o non trovato, creo nuovo log"
              echo "# Log ping Render - Inizializzato $(TZ='Europe/Rome' date '+%Y-%m-%d %H:%M:%S')" > log.txt
            fi
          else
            echo "❌ GIST_ID non configurato"
            echo "# Log ping Render - Inizializzato $(TZ='Europe/Rome' date '+%Y-%m-%d %H:%M:%S')" > log.txt
          fi

      - name: Ping servizio Render
        id: ping
        continue-on-error: true
        run: |
          SITE="https://otello-web.onrender.com/core/wake/"
          TIMESTAMP=$(TZ="Europe/Rome" date '+%Y-%m-%d %H:%M:%S')
          
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" --max-time 10 "$SITE" || echo "TIMEOUT")
          
          # Aggiungi al log
          echo "$TIMESTAMP | HTTP $STATUS" >> log.txt
          
          echo "✅ Ping eseguito: $STATUS"
          echo "📝 Log aggiornato: $(wc -l < log.txt) righe totali"
          
          echo "status=$STATUS" >> $GITHUB_OUTPUT
          echo "timestamp=$TIMESTAMP" >> $GITHUB_OUTPUT

      - name: Invia notifica errore su Telegram
        if: steps.ping.outputs.status != '200'
        run: |
          STATUS="${{ steps.ping.outputs.status }}"
          TIMESTAMP="${{ steps.ping.outputs.timestamp }}"
          
          MESSAGE="⚠️ *Ping fallito*%0A%0AServizio: \`otello-web.onrender.com\`%0ACodice HTTP: \`$STATUS\`%0AOrario: $TIMESTAMP"
          
          curl -s -X POST \
            "https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage" \
            -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
            -d parse_mode="Markdown" \
            -d text="$MESSAGE"

      - name: Aggiorna log su Gist
        if: always()
        continue-on-error: true
        run: |
          GIST_ID="${{ secrets.GIST_ID }}"
          
          if [ -z "$GIST_ID" ]; then
            echo "⚠️ GIST_ID non configurato"
            exit 0
          fi
          
          echo "Carico log su Gist ($(wc -l < log.txt) righe)..."
          
          # Prepara JSON
          LOG_CONTENT=$(cat log.txt | jq -Rs .)
          
          # Aggiorna Gist
          RESPONSE=$(curl -s -X PATCH \
            -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            -H "Accept: application/vnd.github.v3+json" \
            "https://api.github.com/gists/$GIST_ID" \
            -d "{\"files\":{\"ping-log.txt\":{\"content\":$LOG_CONTENT}}}")
          
          # Verifica successo
          if echo "$RESPONSE" | jq -e '.id' > /dev/null 2>&1; then
            echo "✅ Log caricato su Gist con successo"
          else
            echo "❌ Errore caricamento Gist:"
            echo "$RESPONSE" | jq -r '.message // "Errore sconosciuto"'
          fi

      - name: Salva log come artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ping-log-backup
          path: log.txt
          retention-days: 7
